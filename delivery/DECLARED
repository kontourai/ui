[
  {
    "scope": "author:briananderson1222 branch-prefix:fix/self-host-fonts",
    "reason": "Vendors ten woff2 faces (Fraunces, Hanken Grotesk, IBM Plex Mono) with their OFL licenses, adds real @font-face rules, and imports fonts.css from the tokens entry point so a consumer with a restrictive CSP gets brand fonts instead of silent fallbacks (ui#50). Agent-delivered outside builder.build, so no trust bundle exists; Step 1 fresh verify passes. Verification in lieu of a bundle: npm run verify green on Node 22 and 24; npm pack --dry-run confirms all ten faces ship (committed set and packed set diff to empty); zero remote @import survives; tokens/index.css imports fonts.css. Scope is paired with author: per the ADR 0022 2026-07-03 addendum -- branch-prefix alone resolves against a pusher-controlled ref, and dropping the pairing was explicitly rejected in flow-agents#1015.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-26 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T00:05:00Z"
  },
  {
    "scope": "author:kontourai-releases[bot] branch-prefix:release-please--",
    "reason": "release-please automation PR; no agent delivery involved, nothing to reconcile. Verified actor against a live run rather than assumed: release PRs here are opened by the kontourai-releases GitHub App. Paired with author: per the ADR 0022 2026-07-03 addendum -- branch-prefix alone resolves against a pusher-controlled ref. Known and accepted cost: if the release bot's identity rotates, this stops matching and the release train breaks. It will break LOUDLY -- flow-agents#1015 made an unmatched marker a named STALE-SCOPE outcome that prints the failed condition and the resolved actor. Thirteen silent days in flow-agents was the bug; a loud break is the correct outcome.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-26 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T00:20:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/release-automation-exemption",
    "reason": "The governance-file change adding the release-automation entry above. Touches delivery/DECLARED only; no source or behaviour changes and no command-backed agent-delivery claims to reconcile.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-26 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T00:20:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:ci/trust-verify-pin",
    "reason": "Bumps the pinned kontourai/flow-agents trust-verify action SHA to e5f348b, the build that reports which DECLARED condition failed and against what resolved context (flow-agents#1015). Consumers pin this action by SHA, so the reporting improvement reached no consumer until each bumped: ui, survey and kontourai.io were each pinned behind it on three different pre-fix SHAs, and ui hit the exact scenario #1015 was built for and got the old one-line message (flow-agents#1018). Changes one pinned SHA and its trailing comment; no source, no behaviour, no dependency change, nothing for a bundle to reconcile. Verification in lieu of a bundle: the action's own Step 1 fresh verify passes in this run, and the new pin's per-entry DECLARED breakdown is visible in this PR's own Trust Verify log \u2014 this change is its own demonstration. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-26 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T04:05:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:docs/us-spelling-fonts-provenance",
    "reason": "Corrects a British spelling ('artefacts' -> 'artifacts') in tokens/fonts/README.md, which ships inside the npm tarball and so was reaching readers on npm, and in the mirroring script's comment. Documentation and a comment only: no source, no behaviour, no dependency change, nothing for a bundle to reconcile. Verification in lieu of a bundle: npm run verify green on Node 22 and 24 in this PR's own CI. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-26 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T04:30:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:feat/full-suite-product-marks",
    "reason": "Ships product marks for the full suite (ui#58): fieldwork and hachure moved byte-identical from kontourai.io (which documented them as staged for this package), plus 13 new marks drawn to the existing 24x24 / 1.75-stroke / currentColor contract, in both shipped forms (icons/*.svg + productIconPaths with per-product React exports). Agent-delivered outside builder.build in an owner-attended session, so no trust bundle exists; Step 1 fresh verify passes. Verification in lieu of a bundle: npm run verify green on Node 22 and 24 in this PR's own CI (token smoke, build + package-export check, readiness, pack check, browser suite asserting viewBox/stroke/class/a11y markup for all 22 slugs). Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner-directed merge, scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-28T06:10:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/flow-agents-local-kit",
    "reason": "Pins @kontourai/flow-agents 5.5.0 as an exact local devDependency and adds the workflow:sidecar / workflow:validate-artifacts scripts, matching the one proven configuration (the marketing-site repo). With bundle-required now enforced portfolio-wide, this repo demanded delivery bundles while providing no declared means to author them: bundle authoring worked only on machines with a coincidental global kit install, and sandboxed delegates without network access could not produce bundles at all. Dev-tooling-only change: no source, no behaviour, no shipped-dependency change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own CI runs the full verify matrix, and the kit's bins resolve from the local install. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted rollout recommendation)",
    "declared_at": "2026-07-28T21:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:feat/contrast-conformance",
    "reason": "Adds scripts/check-contrast.mjs (WCAG relative-luminance conformance over the kit's own theme scopes, declared pair list, 43 combinations) and wires it into npm run check between tokens and exports. Check-script-and-package.json-only change: no shipped source, no token value changes, no dependency change, nothing for a bundle to reconcile. Agent-delivered in an owner-attended session ('do it all' directive) outside builder.build, so no trust bundle exists; Step 1 fresh verify passes. Verification in lieu of a bundle: this PR's own CI runs the full verify matrix on Node 22 and 24 including the new gate; fault-injection evidence in the PR body \u2014 a degraded dark-theme muted token fails naming scope/pair/ratio/threshold, and a zero-pair scan throws rather than reading as conformance. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-08-13 session: owner-directed 'do it all', scoped-DECLARED-exemption route)",
    "declared_at": "2026-08-13T21:05:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/pnpm-migration",
    "reason": "Canary for the org-wide npm -> pnpm 11 move (ui#69; plan tracked from station#516). Lockfile converted with pnpm import, all 116 resolved versions identical to package-lock.json; dependency install scripts blocked by default with esbuild@0.28.1 the single version-pinned allowBuilds entry; CI installs with pnpm install --frozen-lockfile after pnpm/action-setup; publishing stays on npm publish --provenance and npm run verify stays the verify command so this action's own contract string does not move. Tooling, lockfile and workflow change: no shipped source, no token change, no shipped-dependency change (pack dry-run differs from the published 1.12.0 tarball by exactly the one file main added after the tag), nothing for a bundle to reconcile. Agent-delivered in an owner-attended session outside builder.build, so no trust bundle exists; Step 1 fresh verify passes. Verification in lieu of a bundle: npm run verify green locally (contrast 43/43, package contents 289 files, Playwright 21/21) and in this PR's own CI on Node 22 and 24; fault-injection evidence in the PR body -- a manifest/lockfile mismatch fails with ERR_PNPM_OUTDATED_LOCKFILE and removing the allowBuilds entry fails with ERR_PNPM_IGNORED_BUILDS. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-09-03 session: owner-directed pnpm migration across all repos, scoped-DECLARED-exemption route)",
    "declared_at": "2026-09-03T23:40:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:ci/publish-path-dry-run",
    "reason": "Adds a publish-path dry-run job to ci.yml. After the org-wide npm -> pnpm 11 move (ui#69; plan tracked from station#516), nineteen repositories had proven install, build, test and pack, and none had proven publish: npm publish runs only on a v* tag, against a pnpm-installed tree, under a setup-node that also sets registry-url -- a combination the verify jobs never exercise, and it is that .npmrc which npm publish reads. This job mirrors that environment on every pull request and stops one step short of the registry write. It cannot publish: --dry-run performs no write, the job requests no id-token permission so provenance cannot be minted, and no registry credential is provided. Workflow-only change: no source, no dependency, no lockfile and no shipped content is touched, so nothing for a bundle to reconcile. Agent-delivered in an owner-attended session outside builder.build, so no trust bundle exists; Step 1 fresh verify passes (PASS: npm run verify in this PR's own run). Verification in lieu of a bundle: the job is green on the hosted runner at head 83c4000, and its output is the evidence rather than its exit code -- 'npm notice total files: 289' then 'Publish path proven from a pnpm-installed tree: 289 files assembled', with the registry declining only the already-published version. Fault injections recorded in the PR body: a type error in react/src/Badge.tsx makes prepack fail and the job reports 'assembled no tarball'; crafted logs prove the strict branch rejects a 403 while tolerating only the named version-exists refusal, and rejects a zero-file package that exits zero. The first CI run FAILED and that failure is disclosed rather than hidden: GitHub runs run: blocks as bash -e, which set -uo pipefail does not clear, so npm's expected non-zero exit killed the step and every assertion below it was unreachable -- the gate had no body on CI while passing locally, because I had tested it with bash rather than bash -e. Fixed by capturing the status with || STATUS=$?, and both the happy path and the broken-build injection were re-proven under bash -e. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-09-04 session: owner-directed publish-path gate, scoped-DECLARED-exemption route)",
    "declared_at": "2026-09-04T05:10:18Z"
  }
]
