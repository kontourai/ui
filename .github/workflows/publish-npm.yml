name: Publish NPM

on:
  push:
    tags:
      - "console-kit-v*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: ["22", "24"]
    steps:
      - name: Checkout Console Kit
        uses: actions/checkout@v6
        with:
          path: console-kit

      - name: Checkout Kontour Console
        uses: actions/checkout@v6
        with:
          repository: kontourai/kontour-console
          path: kontour-console

      - name: Checkout Flow
        uses: actions/checkout@v6
        with:
          repository: kontourai/flow
          path: flow

      - name: Checkout Survey
        uses: actions/checkout@v6
        with:
          repository: kontourai/survey
          path: survey

      - name: Checkout Surface
        uses: actions/checkout@v6
        with:
          repository: kontourai/surface
          path: surface

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: console-kit/package-lock.json

      - name: Install
        run: npm ci --no-fund --no-audit
        working-directory: console-kit

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium
        working-directory: console-kit

      - name: Verify
        run: npm run verify
        working-directory: console-kit

  publish:
    needs: verify
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/console-kit-v')
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout Console Kit
        uses: actions/checkout@v6
        with:
          path: console-kit

      - name: Checkout Kontour Console
        uses: actions/checkout@v6
        with:
          repository: kontourai/kontour-console
          path: kontour-console

      - name: Checkout Flow
        uses: actions/checkout@v6
        with:
          repository: kontourai/flow
          path: flow

      - name: Checkout Survey
        uses: actions/checkout@v6
        with:
          repository: kontourai/survey
          path: survey

      - name: Checkout Surface
        uses: actions/checkout@v6
        with:
          repository: kontourai/surface
          path: surface

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          cache: npm
          cache-dependency-path: console-kit/package-lock.json

      - name: Install
        run: npm ci --no-fund --no-audit
        working-directory: console-kit

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium
        working-directory: console-kit

      - name: Verify tag matches package version
        run: |
          PACKAGE_VERSION=$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version")
          if [ "console-kit-v${PACKAGE_VERSION}" != "${GITHUB_REF_NAME}" ]; then
            echo "Tag ${GITHUB_REF_NAME} does not match package.json version console-kit-v${PACKAGE_VERSION}" >&2
            exit 1
          fi
        working-directory: console-kit

      - name: Check published version
        id: published
        run: |
          PACKAGE_NAME=$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).name")
          PACKAGE_VERSION=$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version")
          if npm view "${PACKAGE_NAME}@${PACKAGE_VERSION}" version >/dev/null 2>&1; then
            echo "published=true" >> "${GITHUB_OUTPUT}"
            echo "${PACKAGE_NAME}@${PACKAGE_VERSION} is already published; skipping npm publish."
          else
            echo "published=false" >> "${GITHUB_OUTPUT}"
          fi
        working-directory: console-kit

      - name: Publish public package
        if: steps.published.outputs.published != 'true'
        run: npm publish --access public --provenance
        working-directory: console-kit
